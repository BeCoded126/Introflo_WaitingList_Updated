/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // In Next 14+ the app directory is enabled via top-level flag
  appDir: true,
};

module.exports = nextConfig;
