import React from "react";

export default function Home() {
  return (
    <main
      style={{
        fontFamily: "Satoshi, system-ui, -apple-system, sans-serif",
        padding: 40,
      }}
    >
      <h1>introflo.io</h1>
      <p>Smart intros for care teams.</p>
      <p>
        Scaffold ready — complete env and run <code>npm install</code> then{" "}
        <code>npm run dev</code>.
      </p>
    </main>
  );
}
